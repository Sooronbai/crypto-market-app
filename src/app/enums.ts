export enum PriceDirection {
  up = "Up",
  down = "Down",
}

export enum CurrencyType {
  secondary = "Secondary",
  primary = "Primary",
}

export enum SortDirection {
  asc = "asc",
  desc = "desc",
}
